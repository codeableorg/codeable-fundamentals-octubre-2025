// importaciones (imports)
import { randomBetween, sumar } from "./utils.js";

// código interno
const random = randomBetween(1, 10);
console.log(sumar(20, 30));
console.log(random);

// exportaciones (exports)
